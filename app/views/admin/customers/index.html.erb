<h3 class="offset-2 text title-sub-text">会員一覧</h3>
<div class="container">
 <div class="row mb-4">
  <div class="col-12">
    <div class="row">
　　 <div class="col-12 mx-auto">
      <table class="table table-hover">
       <thead>
        <tr class="table-active text">
         <th>会員ID</th>
         <th>氏名</th>
         <th>メールアドレス</th>
         <th>ステータス</th>
        </tr>
       </thead>
       <tbody>
        <% @customers.each do |customer| %>
        <tr class="light-text">
         <td><%= customer.id %></td>
         <td class="text">
          <%= link_to admin_customer_path do%>
           <%= customer.last_name + customer.first_name %>
          <% end %>
         </td>
         <td><%= customer.email %></td>
         <td>
           <% if customer.is_deleted == true %>
            <span class="text-success text">有効</span>
           <% else customer.is_deleted == false %>
            <span>退会</span>
           <% end %>
         </td>
        </tr>
       <% end %>
      </tbody>
     </table>
    </div>
   </div>
   <div class="mx-auto text">
    <%= paginate  @customers %>
   </div>
  </div>
</div>