<%= form_with model: @genre, url: admin_genres_path, local:true do |f| %>
 <%= f.text_field :name %>
 <%= f.submit "新規登録" %>
<% end %>

<% @genres.each do |genre| %>

 <%= genre.name %></td>
 <%= link_to "Edit", edit_admin_genre_path(genre.id) %>

<% end %>
<%= paginate @genres %>