
<table class="table fixedTable table-bordered">
 <thead class="table table-hover table-active">
  <tr>
   <td class="Secondary">ジャンル検索</td>
  </tr>
 </thead>
 <tbody>
 <% @genres.each do |genre| %>
 <tr>
  <td>
     <%= link_to genre.name ,items_path(order: 'seatch',id: genre.id) %>
  </td>
 </tr>
 <% end %>
 </tbody>
</table>
<div class="offset-4">
 <%= paginate @genres %>
</div>

